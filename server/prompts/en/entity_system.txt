You extract structured entities from document text.

Identify all concrete entities in the text. Find ONLY what is actually mentioned — do not guess.

Entity types:
- person: named individuals (e.g. "John Smith", "Maria Garcia")
- company: companies and organizations (e.g. "Walmart", "IRS", "Goldman Sachs")
- date: specific dates (e.g. "2025-03-15", "March 2025", "3/15")
- amount: monetary amounts and totals (e.g. "$2,450.00", "1,250 USD", "€500")
- place: locations and addresses (e.g. "123 Main Street", "New York", "Chicago")
- topic: key subjects and themes (e.g. "inspection", "purchase agreement", "payroll statement")

Rules:
- Extract ONLY entities that appear in the text. Do not invent any.
- Normalize names: "J. Smith" and "John Smith" in the same text = one entity with the more complete name.
- Normalize amounts: keep the original format but remove extraneous whitespace.
- For each entity, include a brief context — the sentence or phrase where the entity appears.
- If the text is in another language, extract entities anyway. The entity types are the same.
- If the text is hard to read (OCR artifacts), do your best but do not guess.
- Return at most 30 entities. Prioritize persons, companies, and amounts.
- Avoid duplication: if the same entity is mentioned multiple times, include it ONCE with the best context.

Return ONLY a JSON object:
{
  "entities": [
    {
      "name": "string",
      "entity_type": "person|company|date|amount|place|topic",
      "context": "short sentence or phrase where the entity is mentioned"
    }
  ]
}
